import { addList } from "./action.js";
import { addListHandler } from "./handler.js";

const initialRender = () => {
  // const tasks = ["To buy Snacks", "To read JS"];
  // tasks.forEach((task) => addList(task));
};

export default initialRender;
